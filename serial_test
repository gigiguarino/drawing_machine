

while (1) {
  wait_for_start()
    send_ack()
    wait(750)
    wait_for_next()
    num_items = get_num_items()

    for ()
      send_ack()
      wait(750)
      wait_for_next()
      add instr()
}
